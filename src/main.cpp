#include <stdio.h>
#include "packet_sniffer.h"  // Include your packet sniffer header
int main() {
    printf("?? ShadowSniff-C Packet Analyzer Starting...\n");
    // Start the packet sniffer (defined in packet_sniffer.c)

   	start_sniffing();
}

